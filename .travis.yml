# Copied from https://github.com/nim-lang/Nim/wiki/TravisCI
language: c

cache: ccache

matrix:
  include:
    # Build and test against the master (stable) and devel branches of Nim
    # Build and test using both gcc and clang
    - os: linux
      env: CHANNEL=stable
      compiler: gcc

    - os: linux
      env: CHANNEL=stable BACKEND=OpenCL
      compiler: gcc

    - os: linux
      env: CHANNEL=devel
      compiler: gcc

    # On OSX we only test against clang (gcc is mapped to clang by default)
    # Note: for OpenMP, Homebrew will build flame/blis with GCC-5
    # As BLIS is in OSX homebrew, this is an opportunity to test it as well
    - os: osx
      env: CHANNEL=stable BLIS=true
      compiler: clang

  allow_failures:
    # Ignore failures when building against the devel Nim branch
    # Also ignore OSX, due to very long build time and Homebrew/curl SSLRead errors
    - env: CHANNEL=devel
    - os: osx
  fast_finish: true

addons:
  apt:
    packages:
      # On Linux we need OpenBLAS, on OSX Apple Accelerate is present by default
      - libopenblas-dev

before_install:
  # On MacOS flame/blis can be tested as it is an homebrew package
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew update          ; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew install brewsci/science/blis; fi
  - if [[ "$BACKEND" == "OpenCL" ]]; then
      bash ci/opencl_amd_sdk.sh;

      echo "deb http://ppa.launchpad.net/cnugteren/clblast/ubuntu xenial main" | sudo tee -a /etc/apt/sources.list
      echo "deb-src http://ppa.launchpad.net/cnugteren/clblast/ubuntu xenial main" | sudo tee -a /etc/apt/sources.list
      sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys 1F9D5FBEF38CB4E0

      sudo apt-get update;
      sudo apt-get install clblast;
    fi

install:
  - export CHOOSENIM_NO_ANALYTICS=1
  - curl https://nim-lang.org/choosenim/init.sh -sSf > init.sh
  - sh init.sh -y
  - export PATH=~/.nimble/bin:$PATH
  - echo "export PATH=~/.nimble/bin:$PATH" >> ~/.profile
  - choosenim $CHANNEL

script:
    - nimble refresh
    - if [[ "$BACKEND" == "OpenCL" ]]; then
        nimble test_opencl;
      else
        nimble test;
      fi

branches:
  except:
    - gh-pages
